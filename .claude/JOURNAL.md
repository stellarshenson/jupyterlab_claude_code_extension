# Claude Code Journal

This journal tracks substantive work on documents, diagrams, and documentation content.

**Note**: Entries 1-27 have been archived to [JOURNAL_ARCHIVE.md](JOURNAL_ARCHIVE.md).

---

28. **Task [Extended] - Real root cause: pin webpack to 5.106.0, not license-webpack-plugin** (v1.1.10): the 1.1.9 lwp 4.0.2 "fix" was a phantom - 4.0.2 carries the identical bug; CI still red<br>
    **Result**: GitHub Actions `Build` / `Check Release` kept failing on the published 1.1.9 with "Cannot read properties of undefined (reading 'trim')", even though `make publish` succeeded locally. Bisected the actual cause: it is a webpack-version drift, not Node and not the plugin version. `license-webpack-plugin`'s `WebpackInnerModuleIterator.getActualFilename` parses module-federation share identifiers with `filename.split('=')[1].trim()`, expecting `provide module (scope) name@version = request`. webpack changed that identifier in `5.106.1` - `ProvideSharedModule.identifier()` now emits `name@version|request` (a `|`, no `=`), so `split('=')[1]` is `undefined` and `.trim()` throws. Confirmed BOTH `2.3.21` and the latest `4.0.2` do the identical split - 4.0.2 fixes nothing and is the last published version (package unmaintained), so there is no forward fix. Bisected webpack by inspecting `node_modules/webpack/lib/sharing/ProvideSharedModule.js` across versions: `5.106.0` is the last with the parseable `= ` format; `5.106.2` / `5.107.x` use `|`. The reason 1.1.9 "published fine" on Node 25: `make publish` hit hatch-jupyter-builder's skip-if-exists and repackaged stale JS - webpack never re-ran (the committed 1.1.9 lockfile actually pins the broken webpack 5.106.2 + lwp 2.3.21 combo). Fix: pinned `webpack: 5.106.0` in BOTH `resolutions` (jlpm/CI path) and `overrides` (npm/Makefile path), inside `@jupyterlab/builder`'s `webpack ^5.76.1` range; dropped the useless `license-webpack-plugin: 4.0.2` resolution. Regenerated `package-lock.json` + `yarn.lock`. Verified on Node 24.16.0 across all three paths: `jlpm build:prod` ("webpack 5.106.0 compiled successfully"), `python -m build` (wheel + sdist), and `pip install .[test]` (faithful CI path, `third-party-licenses.json` regenerated). Corrected the now-wrong root-cause notes in the project `.claude/CLAUDE.md` Makefile pin and the user-level `jupyterlab-extension` skill (both had blamed Node 24+ / lwp 4.x). With webpack fixed, `make publish` then exposed a SECOND Node-24 transitive break: `duplicate-package-checker-webpack-plugin` (imported at module-load by `@jupyterlab/builder`) pulls `chalk@^2.3.0`, and the build-isolation `jlpm install` hoisted a duplicate `chalk@2.4.2` whose `index.js:228` `module.exports.default = module.exports` threw "Cannot set property default of #<Chalk> which has only a getter" - the dev/`pip install` path had dodged it via different hoisting. Pinned `chalk: 4.1.2` (resolutions + overrides) to dedupe the tree to one copy; `duplicate-package-checker` only uses `chalk.green.bold` / `.reset` / `.white` / `.white.bold`, all unchanged in chalk 4, and chalk 4 has no `module.exports.default` line. Verified `python -m build` (the failing isolated path) now reports "webpack 5.106.0 compiled successfully" and builds wheel + sdist on Node 24.16.0. Built v1.1.10 via `make install`, `enabled OK`.

29. **Task - Silence Copy Path toast** (v1.1.11): the "Copy Path" context-menu item flashed a `Notification.success` every click, which the user found noisy<br>
    **Result**: Removed the `Notification.success(\`Copied: ${path}\`, { autoClose: 2000 })` call from the `claude-code-sessions:copy-path` command in `src/widget.ts` so the click just runs `Clipboard.copyToSystem(path)` and stays silent. The success toast was added in 1.0.59 (entry 19) to give visible feedback that the click registered - matching the spinner-floor pattern - but for a clipboard action the system clipboard itself is the confirmation and the toast adds nothing. Kept the `Notification` import - still used by the `claude-code-sessions:show-in-filebrowser` warning toasts at lines 961 and 982, which signal a real failure mode (folder outside the JL server root). No other behavioural change; pure UX cleanup. `jlpm run lint:check` clean. Built v1.1.11 via `make install`, `enabled OK`.

30. **Task - README warning about the webpack/chalk pins** (v1.1.12): document the non-obvious resolutions/overrides so a future contributor does not "tidy them up"<br>
    **Result**: Added a terse GitHub-style `> [!WARNING]` block to `README.md` between the Install and Uninstall sections naming both pins (`webpack: 5.106.0`, `chalk: 4.1.2`), the exact failure modes (`split('=')[1].trim()` crash from webpack `>= 5.106.1`'s `|` identifier format hitting unmaintained `license-webpack-plugin` injected by `@jupyterlab/builder`; duplicate `chalk@2.4.2` from `duplicate-package-checker-webpack-plugin` crashing on Node 24+ in the build-isolation install), and the symptom (`make publish` and CI fail on `python -m build`). Also empirically re-verified the necessity before writing the note: stripped both pins, regenerated locks, and ran `python -m build` - webpack resolved to `5.107.2`, build crashed with the identical `Cannot read properties of undefined (reading 'trim')`. Restored pins, confirmed clean wheel + sdist on Node 24.16.0. Built v1.1.12 via `make install`, `enabled OK`.

31. **Task - Drop "Run in background" button from launch spinner** (v1.1.13): the launch-spinner modal carried a `Run in background` cancel button that added noise without value<br>
    **Result**: Removed `Dialog.cancelButton({ label: 'Run in background' })` from `_showLaunchSpinner` in `src/widget.ts` (line 567), leaving `buttons: []`. The spinner now shows just the spinner + label and auto-dismisses via `dialog.resolve()` once the terminal spawn completes (or errors). The cancel button was originally there so users could stash a slow spawn behind the panel, but the launch path is fast in practice (one POST to `launch-terminal` then a JL `terminal:open`), so the early-dismiss option created the impression that the spawn was a long background job when it is not. `jlpm run lint:check` clean. Built v1.1.13 via `make install`, `enabled OK`.

32. **Task - Launch spinner stayed open after dropping its button** (v1.1.14): a regression from 1.1.13 - the modal no longer dismissed when the terminal opened<br>
    **Result**: 1.1.13 set `buttons: []` on the launch-spinner Dialog, but the dismiss call at line 460 was `spinner.resolve()`, which in JupyterLab's `@jupyterlab/apputils` Dialog programmatically "clicks" the default button - with no buttons there is nothing to click and the modal hung over the panel forever. Switched the call to `spinner.dispose()` which forcibly closes the Lumino widget regardless of button state, so the `finally` block dismisses the modal on both success and error paths. Updated the `_showLaunchSpinner` docstring to call out the new dismiss contract (`.dispose()` mandatory because `.resolve()` is a no-op without buttons) so the next reader does not reintroduce the bug. `jlpm run lint:check` clean. Built v1.1.14 via `make install`, `enabled OK`.

33. **Task - Galata spinner test rewrite to pass in CI**: test added in 63b3a60 to guard the 1.1.14 dispose() fix timed out in CI waiting for `.jp-ClaudeSessionsPanel-row`<br>
    **Result**: First version mocked `/status` with `{has_claude, version, running_sessions}` but the frontend reads `status.enabled` (see `src/types.ts::IStatusResponse` + `src/index.ts:44`) - undefined → falsy → activate branch logged "panel disabled" and never constructed the widget; `activateById` silently no-op'd, no row ever rendered. Session mock also had wrong shape (`favorite`/`branch` vs `favourite`/`git_branch`; missing `encoded_path`, `summary`, `first_prompt`, `created`, `modified`, `remote_control`). Rewrote `ui-tests/tests/jupyterlab_claude_code_extension.spec.ts`: correct `/status` shape so the panel registers, empty `/sessions`, walk `app.shell.widgets('left'|'right')` for the panel by id, drive `panel._showLaunchSpinner(label)` from a `page.evaluate`, stash the Dialog handle on `window.__claudeSpinner`, then dispose it - the same `try/finally` pattern as `_doResumeInTerminal`. Asserts dialog attaches then detaches under `.dispose()`. Skips the brittle full row-click + `terminal:open` path (CI has no fake terminal). `lint:check` clean.

34. **Task - Test fix follow-up + minimise launch spinner UI** (v1.1.15): round-1 Galata rewrite (entry 33) still timed out in CI because the polling loop hit `app.shell.widgets()` before the extension's async `activate()` had finished awaiting `/status` and called `labShell.add`; separately the user asked to make the spinner popup really minimal<br>
    **Result**: Replaced the brittle `waitForFunction` polling with `page.waitForSelector('#jupyterlab-claude-code-extension', {state:'attached'})` - Lumino assigns the widget id to the side-stack node the moment `labShell.add` runs, so it is a stable readiness signal. Only then walk `widgets('left'|'right')` to grab the panel instance. Pushed as `dc68c4c`. Then stripped the spinner Dialog body to its core: dropped the `.jp-ClaudeSessionsPanel-launchLabel` `<div>` that rendered "Opening &lt;project&gt;..." under the spinner, dropped the matching CSS rule in `style/base.css`, and removed the `label: string` parameter from `_showLaunchSpinner` in `src/widget.ts` (call site in `_doResumeInTerminal` no longer interpolates the session name). Title bar "Opening Claude Code session" stays as the only text - just title + rotating spinner. Updated the Galata test to call `_showLaunchSpinner()` with no arg. `jlpm run lint:check` clean.

35. **Task - Replace failing Galata spinner test with a Jest source-grep** (v1.1.16): the rewritten Galata test still timed out on the v1.1.15 CI run - `#jupyterlab-claude-code-extension` never materialised in the DOM under mocked routes, third consecutive CI red on the integration job<br>
    **Result**: Three Galata iterations (row-click, `waitForFunction` over `app.shell.widgets`, `waitForSelector` by panel id) all timed out without clear telemetry. Pivoted - regression is purely `Dialog({buttons:[]}).resolve()` no-op vs `.dispose()` working, so a source-level invariant captures it faster than any DOM test. Deleted the Galata spinner test, kept the activation-console one. Added Jest `describe('launch spinner dismiss contract')` to `src/__tests__/jupyterlab_claude_code_extension.spec.ts` - reads `src/widget.ts` via `fs.readFileSync`, asserts `spinner.dispose()` present, `spinner.resolve()` NOT, `_showLaunchSpinner` builds `Dialog` with `buttons:[]`. Needed local `declare function require` + `declare const __dirname` (`tsconfig.json` pins `types:['jest']`). Verified guard fires: flipped `dispose()` -> `resolve()`, Jest failed correctly; restored. Tried a runtime `Dialog` test first but `@jupyterlab/apputils` pulls `ui-components/*.tsx` Jest cannot transform here.

36. **Task - Ignore YouTube URLs in CI link checker** (v1.1.17): `Check Release` Build failed at the link-checker stage on the 1.1.16 release runs because `ui-tests/README.md` links to a YouTube video and YouTube returned 429 Too Many Requests<br>
    **Result**: `jupyterlab/maintainer-tools/.github/actions/check-links@v1` walks every markdown link and rate-limits hits intermittently. The offending link is at `ui-tests/README.md:15` (Playwright UI-mode demo, "[that video](https://www.youtube.com/watch?v=jF0yA-JLQW0)"); YouTube's 429 is non-deterministic so the same commit can pass on one run and fail on the next - exactly what happened between the a97dc8c and 52e9ae1 runs which confused the earlier "CI green" call. Added `https://www.youtube.com/.*` and `https://youtu.be/.*` to `ignore_links` in `.github/workflows/build.yml` alongside the existing npmjs and pepy patterns. CI-only change, no package bytes affected; release exists purely to verify the link-checker fix on a fresh run. No code or test touched.

37. **Task - Filter toggle button collapses the search bar by default** (v1.1.18): the panel always rendered a "Filter sessions..." input under the header even when unused, taking permanent vertical real estate above the row list<br>
    **Result**: The always-on search field was costing ~30 px above Favourites/Recent/All for the 90% of clicks that never use it. Added `filterIcon` to `src/icons.ts` (Material `FilterList` SVG, viewBox 24x24, `class="jp-icon3"`, matching the existing `refreshIcon`/`removeIcon` style verbatim). `src/widget.ts::_buildShell` now constructs a filter button next to the refresh button, sets the search input's `hidden` attribute to true by default, and stashes both as `_filterBtn` and `_searchEl` fields. New `_toggleFilterBar()` flips `hidden`, focuses the input on show, clears `_filter` + re-renders on hide so a stashed-away filter does not silently narrow the rows on next open; the button gets `jp-mod-active` while the bar is visible. `style/base.css` adds a `.jp-ClaudeSessionsPanel-iconButton.jp-mod-active` rule using `--jp-brand-color1`. `jlpm run build:lib` clean, `jlpm run lint:check` clean. Built v1.1.18 via `make install`, `enabled OK`.

38. **Task - Filter icon matches the file browser's magnifying glass** (v1.1.19): the v1.1.18 filter toggle button used a Material `FilterList` (three horizontal bars) icon but JupyterLab's file browser uses a magnifying glass for its filter box, so the two readouts were visually inconsistent<br>
    **Result**: Swapped the `filterIcon` SVG in `src/icons.ts` for a verbatim copy of `@jupyterlab/ui-components/style/icons/toolbar/search.svg` - the same magnifying glass the file browser renders at the right of its `FilterBox` (sourced via `FilenameSearcher` -> `searchIcon` in `@jupyterlab/ui-components/lib/components/search.js:7`). The new SVG keeps the existing `LabIcon` registration name (`jupyterlab_claude_code_extension:filter`) so the widget wiring is untouched - `viewBox="0 0 18 18"`, `fill="#616161"`, `class="jp-icon3"` matching the source. No behaviour change to `_toggleFilterBar` or the button's `jp-mod-active` highlight. `jlpm run lint:check` clean. Built v1.1.19 via `make install`, `enabled OK`.

39. **Task - Filter icon is actually the file browser's funnel, not its searcher's magnifier** (v1.1.20): the v1.1.19 swap picked the magnifying-glass from `@jupyterlab/ui-components/style/icons/toolbar/search.svg`, but that icon lives inside `FilenameSearcher`'s input box - the file browser's external filter toggle uses the funnel from `search/filter.svg`, so the readouts still did not match<br>
    **Result**: User pasted the actual rendered SVG from the file browser header: `<path d="M14 12v7.88c.04.3-.06.62-.29.83a.996.996 0 0 1-1.41 0...">` - clearly the funnel from `search/filter.svg`, not the magnifying glass. Re-swapped `filterIcon` in `src/icons.ts` to a verbatim copy of `search/filter.svg` (`viewBox="0 0 24 24"`, `fill="#FFF"` on the path which is effectively inert because `class="jp-icon3"` lets the theme drive the actual colour). `LabIcon` registration name (`jupyterlab_claude_code_extension:filter`) preserved so the widget wiring is unchanged. The earlier mistake was reading `FilenameSearcher`'s React code and assuming the search input's internal icon was the toggle icon - the file browser does not actually have an external magnifier; its filter button is the funnel. `jlpm run lint:check` clean. Built v1.1.20 via `make install`, `enabled OK`.

40. **Task [Extended] - Honour Claude's session name so /rename shows in the panel** (v1.1.21): a session renamed in Claude (e.g. `/rename scandi` for `/home/lab/workspace/kolomolo/sales/proposals`) still displayed the folder basename "proposals" instead of "scandi"<br>
    **Result**: Investigation showed it was never a ranking bug - the latest-wins picker in `session_state_by_cwd` already resolved that cwd to `scandi` (`5078.json`, `updatedAt=1780071050272`, the max of 34 pid records for the cwd). The name was simply discarded: since v1.1.8 (entry 25) `session_state_by_cwd` dropped the `name` field on purpose and `list_sessions` hard-coded `name = basename`, because Claude writes both `/rename` values and its own auto-derived topics into one field with no flag to separate them. Confirmed modern Claude (v2.1.156, the same version that wrote "scandi") still emits auto-topics: 41% of all local pid files have `name != basename` (e.g. `jupyterlab-mermaid-upgrade`, `court-cases`). Per the user's decision to honour the session name regardless, restored reading it: `session_state_by_cwd` now keeps the latest record's `name`; `list_sessions` uses it (`name_source="session"`) and falls back to the folder basename only when it is missing or blank. `src/widget.ts::_displayName` honours the name in `folder` mode; `src/types.ts` `name_source` union is now `'session' | 'basename'`; `schema/plugin.json` presentationMode description rewritten. Tests: replaced the two that asserted the old "ignored" behaviour, added `test_latest_pid_name_wins_after_rename` (mirrors the proposals->scandi data) and `test_blank_pid_name_falls_back_to_basename`. pytest 35/35, jest 6/6, `build:lib` + `lint:check` clean. Known tradeoff: folders where Claude auto-derived a topic now show that topic rather than the bare folder name. Built v1.1.21 via `make install`, `enabled OK`.

41. **Task - Context menu cleanup of parallel sessions** (v1.1.23): projects accumulate parallel session JSONLs over time; user asked for a right-click item showing the extra count in brackets that keeps only the main session and removes the rest (to trash if configured)<br>
    **Result**: `sessions.py` rows now carry `extra_sessions` (count of `*.jsonl` beyond the main one) and new `cleanup_parallel_sessions()` keeps the same session `_resolve_latest` surfaces, removing every other `<sid>.jsonl` plus its sibling `<sid>/` subagent dir via new `_dispose_path` helper - send2trash with permanent-delete fallback, same semantics as `remove_session`; `sessions-index.json` and `memory/` untouched. New `POST sessions/cleanup` (`SessionCleanupHandler` in `routes.py`) honours `ContentsManager.delete_to_trash` and reuses the traversal guard. Frontend: command `claude-code-sessions:cleanup-parallel` with dynamic label `Clean Up Parallel Sessions (N)`, hidden when N=0, placed above Remove; `_cleanupParallel` POSTs then `_fetch()` refreshes the count. README feature list updated (cleanup bullet, funnel-toggled search, /rename note). pytest 42/42 (7 new), jest 6/6, tsc clean. Built v1.1.22 via `make install`.

42. **Task - Cleanup popup with progress bar** (v1.1.25): user wanted the parallel-session cleanup to show a popup with a progress bar reporting progress plus a success or error message, instead of running silently<br>
    **Result**: `_cleanupParallel` in `src/widget.ts` now opens a Lumino `Dialog` ("Clean Up Parallel Sessions") whose body holds a message line and a native `<progress>` element - left valueless so it animates indeterminate while the single `POST sessions/cleanup` is in flight (backend deletes in one call, so no per-file progress exists). On success the bar fills (`bar.value = 1`), message reads "Removed N parallel session(s)." and `_fetch()` refreshes the row count; on error the bar is removed and a red `jp-ClaudeSessionsPanel-cleanupError` message shows the failure. Close button hidden via `.jp-Dialog-footer` display toggle during the request, restored in `finally`. Three CSS rules added to `style/base.css`. Five source-grep contract tests added to the jest spec (progress element, footer toggle, success fill, error class, post-success refresh) - jest 11/11, tsc clean. Built v1.1.24 via `make install`.

43. **Task - New-session header buttons plus Galata coverage** (v1.1.27): user wanted plus icons in the panel header to start a fresh claude session in the file browser's current folder - one normal, one with the context menu's shield for --dangerously-skip-permissions - launching exactly like resume (claude as the pty's only process), just without --resume; follow-up ask: Galata tests<br>
    **Result**: `LaunchClaudeTerminalHandler` now treats `session_id` as optional - absent means `argv = [claude]` (new session), blank still 400s. `src/index.ts` requests optional `IDefaultFileBrowser` (new dependency `@jupyterlab/filebrowser ^4.0.0`); `_newSession` in `widget.ts` resolves `root_dir` + browser-relative path, shows the launch spinner, POSTs `launch-terminal` without `session_id`, attaches via `terminal:open` and registers the tab for reuse. New `addIcon`/`addShieldIcon` in `icons.ts`. Galata: fake `claude` script on PATH via `jupyter_server_test_config.py` so CI registers the panel; two new specs assert both header buttons and that clicking plus yields a `.jp-Terminal` plus a live `/api/terminals` session (xterm canvas defeats DOM text asserts); `playwright.config.js` honours `JLAB_TEST_PORT` to dodge a busy 8888. Three pytest endpoint tests for new/resume/blank session_id. galata 3/3, pytest 45/45, jest 11/11, built v1.1.26 via `make install`.

44. **Task - Single plus button with new-session dropdown** (v1.1.29): user revised the v1.1.27 two-button design - one plus button only, clicking it should present a menu with the normal and Skip Permissions launch choices<br>
    **Result**: dropped the second header button and the composite `addShieldIcon` from `icons.ts`; the remaining plus button now opens a Lumino `Menu` at its bottom-left corner (`getBoundingClientRect` -> `menu.open(rect.left, rect.bottom)`). Two new commands in the shared `CommandRegistry` - `claude-code-sessions:new-session` ("New Claude Session") and `:new-session-dangerous` ("New Claude Session (Skip Permissions)", `shieldIcon`) - both delegate to the existing `_newSession`; the dropdown reuses the `jp-ClaudeSessionsContextMenu` class so styling matches the row context menu. Galata specs reworked to click the button, assert both menu items, then click "New Claude Session" and verify the terminal plus a live `/api/terminals` session. galata 3/3, jest 11/11, tsc clean. Built v1.1.28 via `make install`.

45. **Task - Honour /rename via JSONL custom-title** (v1.1.31): user's `/home/lab` session renamed to "home" still showed "lab" in the panel - find root cause and a permanent fix<br>
    **Result**: diagnosed live via Playwright - panel row and `sessions` API both returned `name: "lab", name_source: "basename"`. Root cause: current Claude Code persists `/rename` as a `{"type": "custom-title", "customTitle": ...}` record appended to the session JSONL (re-appended near the tail on every resume), while the pid files in `~/.claude/sessions/` keep `name: null` - and the backend only read the pid-file name. Added `_scan_jsonl_for_custom_title` to `sessions.py` (128 KiB tail read like the cwd scanner, `"custom-title"` substring prefilter before `json.loads`); `_resolve_latest` attaches the title, `list_sessions` name priority is now JSONL customTitle -> pid-record name (legacy) -> basename. Three pytest cases added (title honoured, title beats stale pid name, tail scan of large file) - pytest 48/48, jest 11/11. Verified against real data: `/home/lab` resolves to "home"/"session" from both source tree and installed wheel. Built v1.1.30 via `make install`; server restart needed to load the new module.

46. **Task - Branch switching submenu plus conversation count** (v1.2.2): user wanted a context-menu submenu to switch a project's main conversation to one of its branches (parallel session JSONLs), 5 most recent plus a searchable "More..." popup, conversation count in brackets by the row name, edge cases covered, acceptance criteria in docs, minor version bump<br>
    **Result**: backend `list_branches` (all branches newest-first, label = custom title > index summary > short sid) and `switch_branch` (touches the JSONL mtime so `_resolve_latest`, cleanup and claude's own --resume picker agree without extra state; re-resolves and reports actual current; `branch_not_found` when the file vanished) behind `GET sessions/branches` and `POST sessions/switch`. Frontend: context menu rebuilt per open (Lumino submenu items lack `isVisible`), "Switch Conversation Branch" submenu capped at 5 with "More... (N total)" opening a search-filtered Dialog list; rows read `name (N)`; switch errors notify and always `_fetch()`. Docs `docs/acc-crit-branch-switching.md` (terse list incl. removed-branch, removed-current, no-op, cwd-inconsistent edges) plus README bullet. pytest 58/58 (10 new), jest 19/19 (8 new), lint clean. Version set to 1.2.0; built v1.2.1 via `make install`.

47. **Task - Last-activity time on session rows** (v1.2.4): user wanted each session row (and the parallel-sessions list) to show when it was last worked on in simple form - "now" within the last minute, then <n>m / <n>h / <n>d ago<br>
    **Result**: `_renderRow` in `src/widget.ts` appends a dim right-aligned `jp-ClaudeSessionsPanel-rowTime` span fed by the shared `_formatRelativeTime(session.file_mtime)`; new CSS rule in `style/base.css` (flex none, `--jp-ui-font-color2`, size0). `_formatRelativeTime` reworked to the requested buckets: "just now" became "now" and the >30-day `toLocaleDateString` fallback was dropped for plain `<n>d ago` - the branch submenu, "More..." popup and tooltip inherit the format automatically since they already call the same helper. Two jest contract tests added (rowTime span wired to the formatter; formatter has now/m/h/d buckets and no date fallback) - jest 21/21, tsc, prettier and lint:check clean. Frontend-only change; built v1.2.3 via `make install`.

48. **Task - Branch entries show short session id** (v1.2.5): user wanted branch submenu and popup entries to carry the conversation name plus short session id, since branches share the project path; acceptance criteria reworked to checklist style<br>
    **Result**: new `_branchDisplayName` helper in `src/widget.ts` renders `name (shortid)` (first 8 chars) in both the "Switch Conversation Branch" submenu and the "More..." popup; the suffix is skipped when the label already is the backend's short-id fallback, avoiding `3f2a1b9c (3f2a1b9c)`. `docs/acc-crit-branch-switching.md` gained Names and Id criteria and was converted to the new checklist format - `[ ]`/`[o]`/`[x]` states with an append-only `log:` sub-bullet per criterion - codified in the new user-level `acc-criterias` skill (detailed fanout mandatory, ask when in doubt, sub-items only when warranted). One jest contract test added - jest 22/22.

49. **Task - Manage Sessions popup delete plus row age emphasis** (v1.2.7): user wanted session deletion inside the switcher popup (select one/many/all, two-step confirm) with the popup always reachable, plus row name colour reflecting activity age<br>
    **Result**: submenu retitled "Switch and Manage Sessions (N)" with an always-present "Manage Sessions... (N)" entry - previously the popup appeared only beyond 5 branches, leaving 2-5-branch projects without management. Popup leads with the badged, untouchable current conversation; checkbox selection (own click zone, selection mode gates row-click switching), select-all over visible filtered rows, `Delete (n)` arming to `Confirm delete (n)`. Backend `delete_branches` in `sessions.py` (never deletes current main, missing files treated as deleted) behind `POST sessions/delete-branches`. Rows < 60 s old colour `--jp-brand-color1`, > 7 d dim to 0.65 opacity; `docs/acc-crit-row-age-emphasis.md` added, branch-switching acc crit all checked. pytest 65/65 (9 new), jest 30/30 (8 new).

50. **Task - Branch Session fork, subdir-cwd switch fix, row columns, popup design language** (v1.2.9): user reported a same-project branch refusing to switch, asked for aligned time/star columns, a paler live dot, a named "Branch Session" fork action, a branch icon on rows, popup restyle per a documented design language, and a README repositioning<br>
    **Result**: switch bug root-caused with real data - `_resolve_latest` only accepted cwds encoding exactly to the project dirname, rejecting legitimate subdirectory tail cwds; new `_project_path_for_cwd` in `sessions.py` accepts subdir cwds (boundary must be a real `/`, sibling `foo-bar` still rejected) and slices the project root back out. "Branch Session..." plus skip-permissions variant fork via `claude --resume --fork-session --session-id <uuid>`; name stamped through new `POST sessions/set-title` (mtime preserved so titling never switches currency). Rows: star column before fixed-width right-aligned time, `now` in brand colour, dot at 0.75 opacity, branch icon + count badge replaces `(N)`. Popup restyled to the share-files design language, distilled into the jupyterlab-extension skill's `design-language.md`. README repositioned as "Claude Code launcher and manager" with the harness principle. Acc-crit docs updated plus two new ones. pytest 74/74, jest 37/37.

51. **Task - Statusline companion CLI and standalone repo** (v1.2.12): user wanted the powerline statusline shareable - a tiny GitHub project following the ecosystem distribution pattern, and a CLI in the extension installing it from that repo with user consent<br>
    **Result**: researched how statuslines are distributed in the Claude Code ecosystem (ccstatusline, claude-powerline, felipeelias/claude-statusline) - standalone CLI tools wired via `settings.json` `statusLine`, README with screenshot top and install one-liner. Created https://github.com/stellarshenson/claude-code-statusline (script genericised - personal `nb_venv_kernels` lookup replaced with basename fallback, README, MIT, real terminal screenshot). Extension gained `jupyterlab_claude_code install-claude-statusline` console script (`cli.py`, `[project.scripts]`): states URL and targets, asks `Proceed? [y/N]` (`--yes` skips), downloads from the repo (nothing vendored - first vendored iteration removed per user), writes executable script, merges `statusLine` into settings preserving other keys; invalid settings JSON or non-script response abort untouched. `docs/acc-crit-statusline-cli.md` added; 8 CLI tests, pytest 82/82.

52. **Task - Time column tuning, panel-wide star alignment, branch-session submenu** (v1.2.17): user flagged dead space between star and time column, then a wrap artefact, then stars drifting out of vertical line across the panel; also wanted the two Branch Session menu items collapsed into one submenu without ellipsis<br>
    **Result**: time column went through three iterations driven by screenshots - 52px felt wasteful, 3.5em fixed wrapped two-digit day labels (`13d ago` bled into the next row), min-width let the column grow and made the star column drift per row; final form is fixed `width: 4em` + `white-space: nowrap` on both `rowTime` and `branchTime`. Stars now line up across the entire panel: `_renderRow` always renders the time slot (empty without `file_mtime`) and section lists gained `scrollbar-gutter: stable` so a scrolling section no longer shifts its columns. Context menu: new `_branchSessionMenu` Lumino submenu titled "Branch Session" (branch icon) with entries "Normal" and "Skip Permissions" (shield icon), replacing two main-menu items; ellipsis dropped. Acc-crit logs appended in `acc-crit-row-columns.md` and `acc-crit-branch-session.md`. jest 37/37, tsc clean.

53. **Task [Short] - Switch icon on sessions submenu** (v1.2.18): user wanted the "Switch and Manage Sessions" submenu to carry an icon like the Branch Session submenu does<br>
    **Result**: new `switchIcon` in `src/icons.ts` (Octicons arrow-switch-16, MIT, `jp-icon3` themed fill) set as `_branchSubmenu.title.icon` in `widget.ts`. jest 37/37, tsc and lint clean; built v1.2.18 via `make install`.

54. **Task - Confirm before removing a Claude project** (v1.2.20): user wanted a confirmation popup before any session removal, since the "Remove from Claude" context-menu item deleted the entire Claude project and every conversation in it on a single click with no prompt<br>
    **Result**: `_remove` in `src/widget.ts` now opens a `showDialog` confirmation before touching anything - title "Remove from Claude", body naming the project via `_lookupName` and stating the whole project plus all its conversations are deleted irreversibly, with `Dialog.cancelButton()` and a red `Dialog.warnButton({ label: 'Remove' })`; removal proceeds only when `confirm.button.accept` is true, otherwise it returns early. The other two removal paths already gate - the Manage Sessions popup uses a two-step `Delete (n)` -> `Confirm delete (n)` button and Clean Up Parallel Sessions opens its own progress dialog - so neither was changed. tsc clean (`Dialog.warnButton` resolves); frontend-only change.

55. **Task - Confirm parallel and individual session removal** (v1.2.21): user clarified the confirmation should also cover the granular removals within a project - cleaning up parallel sessions and deleting individual sessions in the Manage Sessions popup - keeping the project-level "Remove from Claude" dialog added in 1.2.20<br>
    **Result**: `_cleanupParallel` in `src/widget.ts` now opens a `showDialog` confirmation first (names the project and the parallel-session count, Cancel plus red `Dialog.warnButton({ label: 'Remove' })`), aborting on non-accept before the progress dialog runs - previously it removed on click. The Manage Sessions popup's inline two-step `Delete (n)` -> `Confirm delete (n)` button was replaced by a `showDialog` confirmation (title "Delete Sessions", names project and count, red Delete button); deletion calls `_deleteBranches` only when `confirm.button.accept`. The orphaned `confirmArmed` state and its `jp-mod-confirm` toggles were removed from `_showBranchPopup`/`updateControls`. jest 38/38 (delete contract rewritten to assert the popup, new cleanup-confirmation contract), tsc and lint clean.

56. **Task - Force branch session name via claude -n** (v1.2.22): branch sessions named on creation reverted to the parent's name - find why and force the chosen name to stick<br>
    **Result**: root cause - `claude --fork-session` copies the parent JSONL, inheriting its `custom-title`, and claude re-writes that title as a `custom-title` record on every turn; the old post-hoc single append via `sessions/set-title` lost to the inherited title. Verified empirically that `claude -n <name>` makes claude itself write and re-stamp the chosen name as `custom-title` repeatedly (records at lines 0, 9, 14 in a test run), which is exactly what `_scan_jsonl_for_custom_title` resolves. Fix: `launch-terminal` (`routes.py`) accepts an optional `name`, validates it (400 `invalid_name` if blank), and appends `-n <name>` to the claude argv - injection-safe since the init-waiter does `exec "$@"`. `_branchSession` in `src/widget.ts` sends `name: title`; `_stampForkTitle` kept to seed the title and refresh the row promptly while claude starts. Two pytest cases (forced `-n`, blank rejection), jest asserts `name: title` sent. pytest 84/84, jest 38/38, tsc and lint clean.

57. **Task [Extended] - Branch-session defects, copy session id, refresh spinner, acc-crit consolidation** (v1.2.23): two defects - a branch launch warned the name "could not be applied - use /rename" yet rename worked, and a new branch stayed invisible in the panel for minutes - plus two requested features and an adversarial review<br>
    **Result**: root cause of both defects (logged in new `docs/defects-branch-session.md`): claude materialises a forked session's `<forkId>.jsonl` lazily, only on its first turn, not at launch - proven empirically, file absent through 20 s idle and claude refuses a pre-seeded id with "Session ID ... is already in use". DEF-1 fixed by removing the obsolete `sessions/set-title` path (frontend `_stampForkTitle` poll + false warning, backend `SessionSetTitleHandler` + route, `sessions.set_branch_title`, their tests); the name is owned by `claude -n`. DEF-2 is a claude limitation (wontfix-external), mitigated by the new forced-refresh feedback. Added Copy Session ID (context-menu command + per-row `_branchCopyButton` in the Manage Sessions popup via `Clipboard.copyToSystem`) and a forced-refresh panel veil (`_setLoading`, raised only on explicit refresh, the 30 s poll stays silent; veil lives on the root so `_render`'s body wipe can't remove it). `/adversarial-review` caught a CRITICAL - the veil's author `display:flex` beat the UA `[hidden]{display:none}`, pinning it permanently over the panel and swallowing all clicks - fixed by gating the rule on `:not([hidden])`, regression-tested, round 2 SHIP. Consolidated the seven per-feature acc-crit files into one plugin doc `docs/acc-crit-jupyterlab_claude_code_extension.md`. jest 43/43, pytest 79/79, tsc and lint clean.

58. **Task - Simplify the defects doc format** (v1.2.24): user found the structured defect report too heavy and wanted it simpler, but the Contents TOC kept<br>
    **Result**: reformatted `docs/defects-branch-session.md` to a `## Contents` TOC over flat `### DEF-N` sections with `[ ]`/`[x]` marked in place - dropped the `## Open` / `## Fixed` section split and the move-on-resolve ceremony that made it feel bureaucratic. The user-level `defects-tracking` skill was simplified to match (TOC retained, no Open/Fixed split, severity optional) with this doc embedded as its worked example, and the `acceptance-criteria` skill gained a rule to ask consolidated-vs-per-feature before creating the first acc-crit file. TOC validates via `check-toc.py`, prettier clean.

59. **Task - Manage Sessions popup redesigned as a table** (v1.2.25): the popup's per-row delete confirmation rendered a second Lumino dialog detached from the popup, the current-session row was dimmed so it read as disabled, and the user wanted a scrollable table with an accented current row and no confirmation on popup delete<br>
    **Result**: rebuilt `_showBranchPopup` in `src/widget.ts` as a scrollable table - header strip (select-all + live count) over a bordered list; the current conversation is pinned (`position: sticky`) with a 3px brand left-bar, tint and a bold "CURRENT" chip replacing the dimmed treatment, and is non-selectable. Popup delete goes straight to trash (removing the Dialog-on-Dialog) with an "N moved to trash" status and recoverable tooltip; confirmation stays only on Clean Up Parallel Sessions and Remove from Claude. Added per-checkbox aria-labels, an aria-live region, 24px hit targets, focus management and an in-flight busy lock with a disk-truth refetch after delete. Survived 8 rounds of `/adversarial-review` (ux-designer, last 6 no blockers); verified live via `make install` + Playwright Chromium. `style/base.css` and the acc-crit doc updated; jest 45/45, tsc and lint clean.

60. **Task - Conversation-aware terminal reuse plus Open Branched Conversation** (v1.2.26): switching a branch then clicking the row resumed the original conversation, not the switched one (DEF-3); user also wanted to open branches directly and independently, and a quick refresh once a branch is created<br>
    **Result**: root cause - terminal reuse was keyed purely by project cwd, so the still-open `claude --resume <original>` terminal was refocused regardless of which branch was current (a running claude is pinned to one conversation). Backend `terminal-cwd` now reports the running claude's conversation id from `/proc` argv (`--session-id` for a fork, else `--resume`); `_findTerminalForCwd` reuses only a terminal running the wanted conversation - lenient for the current row, strict for opening a branch. New "Open Branched Conversation" submenu (top 5) and per-row Open in the Manage Sessions popup launch branches independently. `_watchForBranch` polls 2 s (bounded) so a fork surfaces in seconds, not on the 30 s poll. `/adversarial-review` (2 rounds) caught the microcache storing the wanted not observed id and a strict "already in use" on the current row - both fixed, round 2 SHIP. jest 56, pytest 88, tsc and lint clean; Galata specs added, verified live via Playwright.

61. **Task - Fix wrong-terminal focus and switch-revert on branched sessions**: clicking a switched-to branch focused a still-open terminal running the original conversation (DEF-4), and an explicit switch reverted to the busier conversation after a refresh (DEF-5)<br>
    **Result**: DEF-4 cause - reuse leniently refocused any cwd-matching terminal whose conversation was unknown (`session_id: null`); live `pgrep` proved claude run with `-c` reports no id, so the original was wrongly grabbed. Fixed: reuse only on a positive id match (dropped the lenient branch and the `strict` flag); New Session now launches `claude --session-id <uuid>` (new `new_session_id` param) so every extension terminal is identifiable. DEF-5 cause - `switch_branch` only touched mtime while `_resolve_latest` ranks by recency; fixed with a durable `.jl-current` pin honoured over recency (cwd-consistent only; a new session clears it; corrupt pins ignored). A 5-round `/adversarial-review` hardened the reuse gate, fork mutual-exclusion and the pin decode guard. jest 57, pytest 98, lint clean; unreleased. `src/widget.ts`, `routes.py`, `sessions.py`.

62. **Task - Live-verify the branch-session fixes and unblock the Galata suite**: confirm DEF-4/DEF-5 work on the restarted server and make the UI tests runnable<br>
    **Result**: after the user restarted JupyterHub (new backend loaded), verified live via the extension API against a throwaway project - `sessions/switch` to the older conversation wrote `.jl-current` and made it current, and bumping another conversation's mtime far newer (simulating activity) did NOT revert it, so the DEF-5 pin holds; `POST launch-terminal` with both `new_session_id` and `session_id` returned 400 (DEF-4 param wired); the served labextension bundle carries `new_session_id`. Diagnosed why Galata hung on every test: the served JupyterLab renders no `role="switch"` element, so galata's `isInSimpleMode` readiness check waits forever at `page.goto`. Overrode `waitForApplication` in `ui-tests/tests/jupyterlab_claude_code_extension.spec.ts` to wait on splash-teardown plus `.jp-LabShell` instead. Galata still cannot execute in this CLI sandbox (it kills spawned jupyter servers, exit 144); it runs in CI.

63. **Task - Get the Galata UI suite running green (7/7)**: the UI tests could not execute - every test hung at galata's readiness check, and the sandbox kills any jupyter server Playwright spawns itself<br>
    **Result**: ran the suite by pre-starting the test server so Playwright reuses it (a spawned jupyter server is killed with exit 144, a pre-started one survives), then fixed three issues. (1) The served JupyterLab renders no `role="switch"` element, so galata's default `waitForApplication` (`isInSimpleMode`) waits forever - the override waits on splash-teardown + `.jp-LabShell`. (2) The context menu does a `sessions/branches` fetch before opening, slower than the 5 s default under headless - raised the wait to 15 s. (3) The "Manage Sessions" popup click matched the "Switch and Manage Sessions" submenu PARENT via `hasText` - filtered to `.lm-Menu-item[data-type="command"]`. Result: 7/7 Galata green alongside pytest 98 + jest 57, lint clean. `ui-tests/tests/jupyterlab_claude_code_extension.spec.ts`.

64. **Task - Demote the "current" chip to plain text in Manage Sessions**: the popup's current-conversation marker was a boxed, uppercase, brand-bordered "CURRENT" pill the user found too prominent ("super weird"), wanting just small normal text saying "current"<br>
    **Result**: stripped `.jp-ClaudeSessionsPanel-branchCurrentBadge` of its border, radius, padding, brand colour, bold weight, uppercase and letter-spacing - now plain `--jp-ui-font-color2` text at `--jp-ui-font-size0`, matching the sibling "Xh ago" time and sitting in the same column; the brand left-bar + tint + pinned position still mark the row. A 2-round ux-designer `/adversarial-review` returned SHIP and flagged the missing programmatic active state, so the current row now sets `aria-current="true"` (the visual cues are otherwise invisible to screen readers); the reviewer advised NOT to brighten the secondary text since that would re-inflate the hierarchy this change removed. jest 58 (+1 contract), lint clean; live-verified on the running server (plain text + aria-current present). `style/base.css`, `src/widget.ts`.

65. **Task - Release branch-session fixes, the running UI suite and current-marker polish** (v1.2.33): publish the accumulated work of entries 61-64 to npm + PyPI<br>
    **Result**: DEF-4 - terminal reuse now requires a positive conversation-id match (the lenient reuse of an unidentified `-c`/bare-claude terminal is gone) and New Session launches `claude --session-id <uuid>` so every terminal is identifiable; DEF-5 - a switch writes a durable `.jl-current` pin honoured over recency (a new session clears it), so the panel no longer reverts to the busier conversation. The Manage Sessions popup's current marker dropped its boxed uppercase brand chip for plain "current" text plus `aria-current`. Backed by pytest 98, jest 58 and the now-running Galata suite (7/7); the DEF-4/DEF-5 work survived a 5-round adversarial review, the current-marker change a 2-round ux-designer review (both SHIP).

66. **Task - Make a created branch become the row's current conversation (DEF-6)** (v1.2.34): a freshly forked branch appeared in the panel after its first turn but the row stayed on the parent it was forked from, never switching to the new branch<br>
    **Result**: root cause - branch creation relied on recency to make the fork current ("the forked JSONL is the newest"), but the parent is the conversation the user is actively in, so it keeps being appended and its mtime overtakes the fork's, making `_resolve_latest` re-pick the parent. Live `pgrep` proved it: the fork ran 4m24s (`--fork-session --session-id 8dc84140 -n delava-stuff`) yet its JSONL was absent (claude writes a fork lazily on the first turn - the known DEF-2 limitation). Fix: the fork launch now writes the durable `.jl-current` pin to the fork id via a new `set_current_pin` (symmetric with `switch_branch`, inverse of a new session's `clear_current_pin`), so the pin overrides recency the moment the fork's JSONL lands; dangling and harmless until then. `routes.py`, `sessions.py`; acc-crit "Becomes current" reworked and DEF-6 registered. pytest 102 (+4), jest 58, lint clean.

67. **Task - Rebuild the Manage Sessions screen in the design language** (v1.2.35): restyle the "Manage Sessions" popup to the Duoptimum Hub design system while following the current JupyterLab theme<br>
    **Result**: restyled the popup (`style/base.css`) to the Duoptimum design language by adding a scoped `--ccs-*` token layer on `.jp-ClaudeSessionsPanel-branchPopup` that maps the design tokens onto JupyterLab `--jp-*` theme vars, so a theme flip restyles the whole screen. The list became a surface card with hairline-separated 30px rows; search gained an accent focus ring; Open is an outlined-accent list button, Copy a wireframe action-icon, times tabular-nums. DOM and behaviour unchanged, so all acc-crit and jest structure contracts hold. Two review fixes: Delete is now an outline in every state (neutral when disabled, error outline when armed, soft tint on hover - never a solid fill); `--ccs-text-subtle` remapped from `--jp-ui-font-color3` to `font-color2` because the theme renders color3 ~#5a5a5a, washing out the times and "current" marker. Verified live via a Playwright preview rendered with the running theme's vars. jest 58, lint clean.

68. **Task - Dim the favourite star marker** (v1.2.36): the favourite star beside starred rows drew too much attention - purely a cognitive-load and colour concern, the user wanted it quieter<br>
    **Result**: the marker had been a bright `--jp-warn-color1` gold star, and a prior recolour attempt over-engineered the fix by rewriting the icon SVG to `fill="currentColor"` plus a `color` override - the user rejected that, wanting the stock JupyterLab icon left untouched. Reverted `src/icons.ts` to the standard `jp-icon3` star (`fill="#616161"`, theme-driven) and dropped the colour override in `style/base.css`, dimming the marker with a single `opacity: 0.5` so it sits quietly against the row text instead of competing with it. No DOM or behaviour change; jest 58, lint clean.

69. **Task - Fix false "remote control active" indicator (DEF-7)** (v1.2.37): the panel lit the green remote-control light for a plain interactive claude that was not under remote control<br>
    **Result**: root cause - `remote_control` was `state.get("live_pid") is not None` in `list_sessions`, i.e. any live claude pid registered for the row's cwd; but claude 2.x writes `~/.claude/sessions/<pid>.json` for EVERY interactive session and signals the remote-control ("bridge") link with a non-null `bridgeSessionId`. Proven on disk: the winning record for `.../ai-assistant` (`14254.json`) had `bridgeSessionId: null` (a live `claude --resume`, idle) yet lit the row, while genuinely-bridged sessions carry `bridgeSessionId: session_...`. Fix: `session_state_by_cwd` now reads `bridgeSessionId` and sets `remote_control` only when the record is both live AND bridged; `list_sessions` consumes the flag. Registered DEF-7 and consolidated the branch-session defects doc into the lean master `docs/defects.md`. pytest 102 green. `jupyterlab_claude_code_extension/sessions.py`.

70. **Task - Rename the `folder` presentation mode to `name`** (v1.2.38): the presentation-mode setting's default option was labelled `folder`, yet since 1.1.8 it has shown the session name (falling back to the folder basename), so the name misdescribed the behaviour<br>
    **Result**: renamed the option to `name`, matching what it does. `schema/plugin.json` enum `["folder", "path"]` -> `["name", "path"]`, default `folder` -> `name`, and the description now states the initial name is taken from the project folder name. `src/widget.ts` `PresentationMode` and `DEFAULT_PRESENTATION_MODE` are now `'name'`; `src/index.ts` maps any non-`path` value to `'name'`, so an existing user's saved `folder` still resolves to the name mode with no migration step. Behaviour is unchanged - only the option name and its description text; no jest or pytest test pinned the old value. jest 58, lint clean.

71. **Task - Default the panel to the right sidebar** (v1.2.39): the `sidebar` setting docked the panel on the left by default, next to the file browser; the user wanted it on the right by default so it does not crowd the file browser<br>
    **Result**: flipped the default from `left` to `right` in two places that must agree - `schema/plugin.json` `sidebar.default` `left` -> `right` (with the description reworded so `right` is the default that keeps clear of the file browser), and the `currentSidebar` fallback in `src/index.ts` that docks the widget when settings fail to load or the registry is absent. The setting still offers both sides and an existing user's explicit choice is untouched; only the unset default moves. No README or test pinned the side (the Galata suite opens the tab on either sidebar), so nothing else changed. jest 58, lint clean.

72. **Task - Fix the broken context-menu divider and add a favourites icon** (v1.2.40): the separator under "Copy Session ID" rendered as two misaligned segments - the line under the icon column sat at a different height than under the text - and the "Add/Remove Favorites" item had no icon<br>
    **Result**: root cause - the Lumino menu is a `display: table` and JupyterLab draws a separator as a `border-top` on an `::after` inside every cell of the row (`.lm-Menu-item[data-type='separator'] > div::after`, `top: 4px`), so all cell segments must share one baseline to read as a single line. The extension's `.lm-Menu-itemIcon { vertical-align: middle }` (added to centre real icons) also hit the separator's icon cell, offsetting its segment from the label cell's baseline default. Scoped that rule with `:not([data-type='separator'])` so the separator's cells realign (`style/base.css`), and gave the `toggle-favourite` command `icon: starFilledIcon` - the same star used on starred rows (`src/widget.ts`). lint clean.

73. **Task - Gate the remote-control indicator on freshness (DEF-8)** (v1.2.41): the panel lit "remote control active" for `jupyterlab_advanced_image_viewer_extension`, a session last active 17 days ago; the DEF-7 gate (live pid AND non-null `bridgeSessionId`) was necessary but not sufficient<br>
    **Result**: root cause - claude leaves `bridgeSessionId` set in `~/.claude/sessions/<pid>.json` after the bridge disconnects while the interactive process keeps running, so a live-but-abandoned session (winning record `25599.json`, alive, 404h stale) still passed. Proven from disk that `file mtime == updatedAt` for every session - there is no idle heartbeat; the pid file is rewritten only on a busy/idle transition, which fires on each turn whether local or remote (confirmed on this very bridged session, whose `updatedAt` advanced as it worked). So freshness is the only "driven now" signal. Fix: `session_state_by_cwd` also requires `updatedAt` within `REMOTE_CONTROL_FRESH_MS` (1h). Reproduced zombie-only state reads False, a fresh busy bridge True; pytest 102, DEF-8 registered. `jupyterlab_claude_code_extension/sessions.py`.

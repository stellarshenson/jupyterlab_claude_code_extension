# Defects - Claude Code Extension

`[ ]` open, `[x]` fixed. Dated notes under each track how it evolved; depth lives in `.claude/JOURNAL.md`.

## Contents

- [DEF-1: Branch name not applied at launch, false "use /rename" warning](#def-1-branch-name-not-applied-at-launch-false-use-rename-warning) - fixed
- [DEF-2: Branched session missing from panel until first turn](#def-2-branched-session-missing-from-panel-until-first-turn) - fixed
- [DEF-3: Switching a branch then clicking the row resumes the original conversation](#def-3-switching-a-branch-then-clicking-the-row-resumes-the-original-conversation) - fixed
- [DEF-4: Switched branch row focuses an unidentified terminal running another conversation](#def-4-switched-branch-row-focuses-an-unidentified-terminal-running-another-conversation) - fixed
- [DEF-5: Switched branch reverts to the most-active conversation on refresh](#def-5-switched-branch-reverts-to-the-most-active-conversation-on-refresh) - fixed
- [DEF-6: Created branch appears but is not made the row's current conversation](#def-6-created-branch-appears-but-is-not-made-the-rows-current-conversation) - fixed
- [DEF-7: Panel shows "remote control active" for a session that is not remote-controlled](#def-7-panel-shows-remote-control-active-for-a-session-that-is-not-remote-controlled) - fixed
- [DEF-8: "remote control active" stays lit for a bridged session gone stale](#def-8-remote-control-active-stays-lit-for-a-bridged-session-gone-stale) - fixed
- [DEF-9: opening an already-open session spawns a duplicate claude on the same conversation](#def-9-opening-an-already-open-session-spawns-a-duplicate-claude-on-the-same-conversation) - open
- [DEF-10: session opened outside the panel keeps its terminal tab uncoloured](#def-10-session-opened-outside-the-panel-keeps-its-terminal-tab-uncoloured) - fixed
- [DEF-11: terminal tab loses its tint once a daemon fork outranks the real conversation](#def-11-terminal-tab-loses-its-tint-once-a-daemon-fork-outranks-the-real-conversation) - fixed
- [DEF-12: a short section self-scrolls when 33vh caps it below its content](#def-12-a-short-section-self-scrolls-when-33vh-caps-it-below-its-content) - fixed
- [DEF-13: a row whose conversation a background agent owns cannot be opened](#def-13-a-row-whose-conversation-a-background-agent-owns-cannot-be-opened) - fixed
- [DEF-14: a background job with no live worker gets a false bg chip and an attach that resurrects it](#def-14-a-background-job-with-no-live-worker-gets-a-false-bg-chip-and-an-attach-that-resurrects-it) - fixed

### DEF-1: Branch name not applied at launch, false "use /rename" warning

- [x] the panel warned `name "..." could not be applied - use /rename` after a branch launch, yet a manual `/rename` worked; cause: `claude -n` owns the name (written on the fork's first turn) but the obsolete `sessions/set-title` poll 404'd for 30s on the not-yet-written fork file and fired a false failure; fix: removed the set-title path (frontend poll + warning, backend handler + route, `set_branch_title`, tests); `src/widget.ts`
  - 2026-06-21 reported: "name 'home-accessories' could not be applied - use /rename -> and the rename was actually successful"
  - 2026-06-21 fixed: name owned solely by `claude -n`; see [acc-crit Branch Session](acc-crit-jupyterlab_claude_code_extension.md#branch-session)

### DEF-2: Branched session missing from panel until first turn

- [x] a new branch stayed out of the panel (even after a manual refresh), appearing only minutes later; cause: claude writes a forked session's `<forkId>.jsonl` lazily on its first turn, not at launch (proven: absent through 20s idle; claude refuses a pre-seeded id with "already in use"), and the panel lists only files on disk; wontfix-external (claude limitation), mitigated by the fast watcher + refresh spinner; `src/widget.ts`
  - 2026-06-21 reported: "branched session did not show in panel, not even after refresh; only after a few minutes appeared"
  - 2026-06-22 resolved: claude limitation; `_watchForBranch` (2s cadence) surfaces the row seconds after the JSONL lands; see [acc-crit Open Branched Conversation](acc-crit-jupyterlab_claude_code_extension.md#open-branched-conversation)

### DEF-3: Switching a branch then clicking the row resumes the original conversation

- [x] after switching the row to another conversation, clicking the row landed back in the ORIGINAL; cause: terminal reuse was keyed purely by project cwd, so the still-open `claude --resume <original>` terminal was refocused regardless of which branch was current (a running claude is pinned to one conversation); fix: `terminal-cwd` reports the running claude's conversation id (argv `--session-id`/`--resume`), and reuse refocuses only a same-conversation terminal; `jupyterlab_claude_code_extension/routes.py`, `src/widget.ts`
  - 2026-06-22 reported: "switched to another branch - and clicking, I get to the original session, not the branch"
  - 2026-06-22 fixed: conversation-aware reuse; "Open Branched Conversation" submenu + popup Open added; see [acc-crit Open Branched Conversation](acc-crit-jupyterlab_claude_code_extension.md#open-branched-conversation)
  - 2026-06-22 regressed (DEF-4): lenient reuse of an unidentified terminal - superseded by DEF-4's strict-on-identity rule

### DEF-4: Switched branch row focuses an unidentified terminal running another conversation

HIGH - lands the user in the wrong conversation.

- [x] after switching the row to another branch, clicking it focused a still-open terminal running a DIFFERENT conversation until that terminal was closed; cause: lenient reuse refocused any cwd-matching claude terminal whose conversation id was unknown (`session_id: null`) - a claude started `-c`/bare reports null and was taken as a match; fix: reuse only on a positive conversation-id match, and New Session launches `claude --session-id <uuid>` (new `new_session_id` param) so every extension terminal is identifiable; `src/widget.ts`, `jupyterlab_claude_code_extension/routes.py`
  - 2026-06-22 reported: "clicking 'workspace-fix' moves me to the 'workspace' tab; once 'workspace' closed, 'workspace-fix' opened"
  - 2026-06-22 fixed: positive-id-match reuse; hardened over a 5-round adversarial review; Galata 7/7; see [acc-crit Open Branched Conversation](acc-crit-jupyterlab_claude_code_extension.md#open-branched-conversation)

### DEF-5: Switched branch reverts to the most-active conversation on refresh

- [x] after switching, the panel reverted the row's current back to the original conversation after a refresh; cause: `switch_branch` persisted the choice only via the chosen JSONL's mtime and `_resolve_latest` ranks by newest mtime, so continued work in the original bumped its mtime above the switched branch; fix: `switch_branch` writes a durable per-project pin (`.jl-current` holding the chosen session id) honoured over recency when the JSONL exists and is cwd-consistent; a new session clears it, a corrupt pin is ignored; `jupyterlab_claude_code_extension/sessions.py`
  - 2026-06-22 reported: "after some time switches back to 'workspace' (from workspace-fix)"
  - 2026-06-22 fixed: durable `.jl-current` pin (written only for cwd-consistent branches); see [acc-crit Branch Switching](acc-crit-jupyterlab_claude_code_extension.md#branch-switching)

### DEF-6: Created branch appears but is not made the row's current conversation

- [x] a freshly created named branch, once its JSONL materialises, shows in the panel but the row's current stays the PARENT it forked from; cause: creation relied on recency, but the actively-written parent keeps overtaking the fork's mtime, so `_resolve_latest` re-picks the parent; fix: the fork launch writes the `.jl-current` pin to the fork id (`set_current_pin`, symmetric with `switch_branch`), honoured over recency the moment the fork JSONL lands; dangling and benign until then; `jupyterlab_claude_code_extension/routes.py`, `jupyterlab_claude_code_extension/sessions.py`
  - 2026-06-23 reported: "started a branched session from workspace ... over 2m passed - still no branch icon and not switched to"
  - 2026-06-23 fixed: `set_current_pin` at fork launch (reuses the DEF-5 pin); pre-first-turn invisibility stays (DEF-2, external); see [acc-crit Branch Session](acc-crit-jupyterlab_claude_code_extension.md#branch-session)

### DEF-7: Panel shows "remote control active" for a session that is not remote-controlled

- [x] the panel lights the green "remote control active" indicator for a plain interactive claude that is not under remote control (e.g. `/home/lab/workspace/delaval/cognitive-platform/ai-assistant`); cause: `remote_control` was computed as `state.get("live_pid") is not None` (`sessions.py`) - any live claude pid registered for the cwd - but claude 2.x writes `~/.claude/sessions/<pid>.json` for EVERY interactive session and signals the remote-control ("bridge") link with a non-null `bridgeSessionId`, which `session_state_by_cwd` ignored; fix: `session_state_by_cwd` reads `bridgeSessionId` and sets `remote_control` only when the record is both live AND bridged; `jupyterlab_claude_code_extension/sessions.py`
  - 2026-07-01 reported: "/home/lab/workspace/delaval/cognitive-platform/ai-assistant shows 'remote control active' - green light, while it is not"
  - 2026-07-01 root-caused: the winning record for the cwd, `~/.claude/sessions/14254.json`, has `bridgeSessionId: null` (a live `claude --resume`, status idle) yet lit the row; genuinely-bridged sessions carry `bridgeSessionId: session_...` (e.g. this session's `15779.json`), so pid-liveness overcounted remote control
  - 2026-07-01 fixed: `remote_control` requires a live pid AND a non-null `bridgeSessionId`; the DEF-7 case (live claude, bridge null) reads False; pytest 102 green

### DEF-8: "remote control active" stays lit for a bridged session gone stale

- [x] the panel lights the green "remote control active" indicator for a bridged session that is no longer under remote control (e.g. `/home/lab/workspace/private/jupyterlab/jupyterlab_advanced_image_viewer_extension`, last active 17 days ago); cause: claude leaves `bridgeSessionId` set in `~/.claude/sessions/<pid>.json` after the bridge disconnects while the interactive process keeps running, so the DEF-7 gate (live pid AND non-null `bridgeSessionId`) still passes a stale zombie; there is no idle heartbeat - the pid file is rewritten only on a busy/idle status transition, which fires on every turn whether local or remote - so `updatedAt` freshness is the only signal the bridge is being driven now; fix: `session_state_by_cwd` also requires the record to be fresh, active within `REMOTE_CONTROL_FRESH_MS` (1 hour); `jupyterlab_claude_code_extension/sessions.py`
  - 2026-07-03 reported: "jupyterlab_advanced_image_viewer_extension appears to have remote control even if it was not active"
  - 2026-07-03 root-caused: the winning record `~/.claude/sessions/25599.json` is alive but 404h stale with a leftover `bridgeSessionId`; `file mtime == updatedAt` for every session confirms no idle heartbeat, so bridged-ness alone is not liveness; the bridged flag persists across pids and days
  - 2026-07-03 fixed: `remote_control` requires live pid AND non-null `bridgeSessionId` AND `updatedAt` within 1h; reproduced the zombie-only state reads False, a genuinely fresh busy bridge reads True; pytest 102 green

### DEF-9: opening an already-open session spawns a duplicate claude on the same conversation

- [ ] **DEF-9: opening an already-open session spawns a duplicate claude, not a reuse** - clicking a row whose session is already live sometimes errors, sometimes opens a second tab; the plugin does NOT mint a clone id (resume issues `claude --resume <same-id>`, `routes.py:538`) - the duplicate is a SECOND claude process concurrently resuming the SAME conversation after terminal reuse missed the real lock-holder; cause: `_findTerminalForCwd` (`widget.ts:820`) scans only JL terminals and gates reuse on a positive argv-id match, so it cannot see a conversation held (a) headless as a remote-control claude (direct child of `jupyter-labhub`), (b) by a claude-daemon background worker (`claude daemon run` -> `bg-pty-host` -> `--fork-session`), or (c) in a terminal started bare / `-c` (no id in argv); on the miss it spawns `claude --resume <id>` which collides with the live lock-holder; amplifier: `_resolve_latest` can pick a daemon `--fork-session` JSONL as the row's representative, so the row points at a transient fork; `src/widget.ts`, `jupyterlab_claude_code_extension/routes.py`
  - 2026-07-13 reported: "when opening already opened session - sometimes I get error, and sometimes it opens second tab (`stars-ultranova-web`); error (`knowledge-graph-foundry`)"; then "claude creates separate conversations (clones) and maybe it is actually the plugin"
  - 2026-07-13 investigated: live process ancestry - stars `518b5b18` runs headless under `jupyter-labhub` (pid 175849, `claude --resume 518b5b18`) while its one terminal (pid 27439) runs a bare `claude`; kgf row points at `f0c974c6`, a claude-daemon `--fork-session` worker (pid 8924), while its terminal (pid 1869) runs `claude -c` on a different conversation `fc7e72c3`
  - 2026-07-13 root-caused: NOT plugin cloning - stars has a single `518b5b18.jsonl` despite repeated opens, and kgf's 2nd JSONL is claude's own daemon fork; the "separate conversations" are two claude processes concurrently resuming one id (claude 2.x permits it) after a reuse-miss; the plugin's role is spawning the duplicate `--resume`, never minting a new id
  - 2026-07-13 fix options (not implemented): (1) backend - when a terminal's claude carries no argv id, confirm its conversation from the claude pid's `~/.claude/sessions/<pid>.json` sessionId so bare / `-c` / TUI-resumed terminals become reusable; (2) frontend - before spawning, if the wanted id is live in a pid file that is not a focusable terminal (headless / daemon), refuse with an "active elsewhere" message instead of a doomed duplicate; (3) drop daemon `--fork-session` transients from `_resolve_latest` representative selection; deferred - the whole-package build is currently gated on `jupyterlab_colourful_tab_extension` 1.0.19 (colour feature) and this reuse path is adversarial-hardened (DEF-3/4/5/6), so a fix needs the build unblocked and Galata verification before shipping

### DEF-10: session opened outside the panel keeps its terminal tab uncoloured

- [x] a Claude session opened outside the panel (`jupyterlab_passkey_extension`, session `6aeb1045`) kept its terminal tab uncoloured, though the backend resolved the session's colour and panel-launched terminals tinted correctly; cause: `_reconcileTerminalColours` iterated `_terminalsByPath` - the plugin's own launch cache - so a terminal the plugin did not open was invisible to the tint even though `/sessions` carried its `color`; fix: the reconcile now walks JupyterLab's `_terminalTracker` (every open terminal) and re-resolves each terminal's conversation on every pass via the `terminal-cwd` probe (no identity cache, so an in-place conversation switch is never pinned stale); colour match is exact-conversation-id-only (clears rather than borrowing a sibling branch's colour), with a longest-prefix cwd fallback for a bare `claude`; `src/widget.ts`
  - 2026-07-15 reported: "jupyterlab_passkey_extension - doesn't take colour, why?" (session `6aeb1045`)
  - 2026-07-15 root-caused: colour correct end to end (backend resolved `orange`, `orange -> peach` mapping valid); only the frontend apply was scoped to `_terminalsByPath`, never the full terminal tracker
  - 2026-07-15 fixed: tracker-wide reconcile, probe-per-pass, exact-id match with cwd fallback; survived two adversarial-review rounds (fixed sibling-colour borrow, stale-pin, nested-project prefix); jest 58, tsc + lint clean (v1.2.43)
  - 2026-07-16 note: the "no identity cache" claim above was never true in code - `_interrogateTerminal` short-circuited on `_terminalsByPath`; removed under DEF-11

### DEF-11: terminal tab loses its tint once a daemon fork outranks the real conversation

- [x] a session's terminal tab was tinted, then lost its colour after some time (`jupyterlab_passkey_extension`, real conversation `6aeb1045`); cause: regression from the DEF-9 case (c) fix (v1.2.45) - reading the true session id from `~/.claude/sessions/<pid>.json` made a bare `claude -c` terminal report its real conversation id, moving it out of `_colourForTerminal`'s cwd fallback (where it had been tinted, by accident) into the exact-conversation-id branch; that branch matches against the panel ROWS, and a row carries only the project's representative conversation, which `_resolve_latest` flips to any newer JSONL - a daemon `--fork-session` worker (`1fe03df7`, mtime 12:21) outranked the real session (`6aeb1045`, mtime 12:13) - so the terminal matched no row and the exact-id miss cleared the tint; fix: `terminal-cwd` now returns the colour of the conversation the terminal actually RUNS, read from that conversation's own JSONL (`_session_colour`), and `_colourForTerminal` uses it directly instead of resolving through the rows; cwd fallback remains only for a terminal whose conversation cannot be read; `jupyterlab_claude_code_extension/routes.py`, `src/widget.ts`
  - 2026-07-16 reported: "regression - jupyterlab_passkey_extension is getting its tint removed after some time, why?"
  - 2026-07-16 root-caused: proven on live data - pid 3426 (`claude --dangerously-skip-permissions -c`) carries no argv id but pidfile id `6aeb1045`; 0 rows matched it, the row having flipped to daemon fork `1fe03df7`; old path (argv None -> cwd fallback) tinted orange, new path (exact-id miss) returned null
  - 2026-07-16 fixed: colour resolved per-conversation, not per-row - `_session_colour('6aeb1045')` returns `orange` on live data; also dropped `_interrogateTerminal`'s `_terminalsByPath` short-circuit, which pinned a panel-launched tab to its launch-time conversation; pytest 111, jest 64, tsc + lint clean
  - 2026-07-16 review: qa-engineer adversarial review (2 rounds) - round 1 `BLOCKERS (3)`, all confirmed by mutation and fixed: the `color` wire key was pinned at no tier (deleting it reproduced DEF-11 with pytest green -> added an async `jp_fetch` handler test); the new jest tests were exact-text regex pins, green on the DEF-11 symptom and RED on a correct refactor (-> extracted the pure logic to `src/colour.ts`, JupyterLab-free and executable, now 100% line-covered, and replaced the pins with 6 tests that call it); `session_id` reached `Path.glob` unescaped (-> `glob.escape` + test). Round 2 `SUITE LOAD-BEARING`, nothing blocking
  - 2026-07-16 follow-ups (filed, not fixed here): `_scan_jsonl_for_agent_color`'s 128 KiB tail window can age a colour record out as a transcript grows, clearing a tint with no fallback - pre-existing since 1.2.42, reproduced, NOT this bug's cause (the real record measured 22320 bytes from EOF); Galata asserts nothing about the tint, the only rung that could have caught DEF-10 and DEF-11; `src/widget.ts` remains 0% covered - the other 58 jest tests are source-text greps that cannot fail on behaviour

### DEF-12: a short section self-scrolls when 33vh caps it below its content

- [x] a Recent section with only two rows drew a real (overflowing) scrollbar in a short viewport, despite few conversations; TRUE cause (established after two wrong theories, see notes): `.jp-ClaudeSessionsPanel-section` was `max-height: 33vh`, which is viewport-relative, so when the window was short enough that `33vh` fell below the section's own content height (~68px for header + 2 rows, i.e. viewport under ~206px) the section was capped under its content and its list overflowed; fix (v1.2.58, layout redesign): the cap moved to the LIST as `clamp(5 rows, 33vh, 10 rows)` via `--ccs-row-height` - the 5-row floor makes the symptom structurally impossible - with the body as scroll safety valve (`overflow: hidden auto`), `all` shrinkable only to a header+3-rows floor, sticky section headers, and body scrollTop preserved across re-renders; `style/base.css`, `src/widget.ts`
  - 2026-07-24 reported: "when there are only 2 recent conversations, recent conversations get super short but annoying scrollbar; despite there'd be almost no other conversations"
  - 2026-07-24 root-caused: measured in headless Chrome against the real `base.css` - Recent (2 rows) reports `scrollHeight == clientHeight` (`overflows=false`) yet `offsetWidth - clientWidth == 15`, i.e. 15px of gutter reserved in a list that cannot scroll; confirmed JupyterLab ships the global track-painting rule that fills it
  - 2026-07-24 fixed: transparent `::-webkit-scrollbar` / `::-webkit-scrollbar-track:vertical` scoped to `.jp-ClaudeSessionsPanel-list`; re-measured - non-overflowing and overflowing sections keep an identical time-column edge (alignment intact) and a 40-row list still overflows and keeps its thumb; jest 64, lint clean
  - 2026-07-24 caveat: the disappearance of the painted track was NOT visually confirmed - headless Chrome suppresses scrollbar painting in screenshots, so the fix rests on the measurement plus selector specificity (class-scoped beats JupyterLab's unscoped rule); needs an eyeball in a real browser
  - 2026-07-24 reopened (v1.2.54 shipped, still a problem): user retested, incl. another system, `problem-cc.png` shows a full scrollbar WITH ARROW BUTTONS and a thumb on a 2-row Recent; the v1.2.54 transparent-`track` fix blanked only the track, never the scrollbar BUTTONS, so a classic (Windows-style) arrow-button scrollbar still renders in the reserved gutter - the earlier fix helped only overlay-scrollbar platforms (Linux/Mac), where the gutter is blank anyway, which is why it looked fixed locally
  - 2026-07-24 root-caused (live DOM, real running panel via CDP with the hub token): drove the actual JupyterLab, trimmed Recent to 2 rows - `scrollHeight == clientHeight` (`overflow=false`), gutter 15px reserved; proven the content never overflows at viewport heights 340-900, so the scrollbar is purely the reserved gutter rendered as native scrollbar chrome (track + buttons) on classic-scrollbar platforms; only the `all` section escaped because `flex: 1 1 0` makes it far taller than its content
  - 2026-07-24 refixed: lists now carry JupyterLab's own `jp-scrollbar-tiny` utility (`src/widget.ts`), a 4px buttonless transparent-track scrollbar - a 4px webkit scrollbar cannot draw arrow buttons, so the reserved gutter paints nothing until real overflow, on every platform; removed the superseded v1.2.54 track-only CSS from `style/base.css`; verified live by injecting the class into the running panel - gutter drops to 10px, all three sections keep an identical time-column right edge (1229px, alignment intact), Recent(2) stays non-overflowing, All(95) still scrolls; jest 64, lint clean
  - 2026-07-24 caveat: the arrow-button removal was NOT visually confirmed on a classic-scrollbar (Windows) system from here - headless Linux Chrome uses overlay scrollbars and suppresses the paint; the fix rests on the live measurement plus the structural fact that a 4px scrollbar has no buttons (and is JupyterLab's own remedy for exactly this); needs the user's eyeball on the reporting system after install
  - 2026-07-24 CORRECTION - both prior root causes above were WRONG: an isolation test with classic scrollbars forced (`--disable-features=OverlayScrollbar`) proved a non-overflowing element with `scrollbar-gutter: stable` reserves BLANK space and paints NO scrollbar chrome (no track, no buttons) on any platform; a reserved gutter never renders a scrollbar. So `problem-cc.png` (scrollbar with buttons AND a thumb) is GENUINE overflow, not gutter chrome. The `jp-scrollbar-tiny` refix was reverted - it would only have thinned a real scrollbar, not removed it
  - 2026-07-24 root-caused (for real, live panel via CDP with classic scrollbars): the true trigger is `max-height: 33vh` on the section. `33vh` is viewport-relative; when the window is short enough that `33vh` falls below a section's own content height, the section is capped under its content and its list scrolls. Measured the exact threshold live: a 2-row Recent (~68px section) overflows once viewport height drops below ~206px (`33vh` < ~68px) - a short window, split view, or heavy browser zoom. `all` never hits it because `flex: 1 1 0` makes it far taller than its content
  - 2026-07-24 fixed (correctly): `max-height: 33vh` -> `max-height: max(33vh, 200px)` on the section (`style/base.css`), a 200px floor (~8 rows) so a section with a handful of rows never self-scrolls; on a normal-height window 33vh already exceeds 200px so nothing changes there; also removed the misdiagnosed v1.2.54 transparent-track CSS. Verified live with classic scrollbars forced across viewport heights 900/400/180/150: Recent(2) and Favourites(5) `overflow=false` at every height (was true at <=180 before), All(95) still scrolls; screenshot at vh=180 shows a clean 2-row Recent with no scrollbar; jest 64, lint clean
  - 2026-07-24 lesson: two shipped/attempted fixes (v1.2.54 transparent track, then jp-scrollbar-tiny) were built on a "reserved gutter paints chrome" theory that a 5-minute isolation test refuted; the reproduction (short viewport) was the missing evidence all along - reproduce the symptom before theorising the cause
  - 2026-07-24 attempted fix (`max-height: max(33vh, 200px)`, a 200px floor on the cap): verified live that it stops a SMALL section self-scrolling, shipped to adversarial review (architect + bug-hunter, Mode 2) BEFORE commit
  - 2026-07-24 REVERTED after adversarial review - both reviewers independently found the floor clips content: sections are `flex: 0 0 auto` (flex-shrink 0), so a section that hits the 200px floor freezes at 200px and cannot shrink; the `all` section is `flex: 1 1 0` (basis 0, so it cannot hold space against non-shrinkable siblings) inside an `overflow: hidden` body, so in a short window a full Favourites/Recent fills the body and pushes `all` off the clipped bottom edge - its rows become UNREACHABLE with no scrollbar anywhere (bug-hunter measured it live: window 340x250, Recent(10) alone fills the 163px body, All visibleHeight=0). Strictly worse than the scrollbar it fixed, and fires across a wider window range. My live verification of the floor only exercised small sections (Recent2/Fav5), so it never hit the large-section clipping - the lesson: test the LARGE-content case, not just the reported small one
  - 2026-07-24 candidate sweep (live, headless Chrome, sections trimmed, viewports 800/400/250): `33vh` revert - small Recent fine, `all` can still reach 0 in extreme short+full cases (pre-existing, marginal); `flex:0 1 auto` shrink-capable with `all: flex 1 1 auto` - never hides content BUT squeezes even a 2-row Recent to ~20px and makes it scroll at every viewport (worse for the common case). No one-line CSS change satisfies all three invariants (small section never self-scrolls; large section scrolls internally; nothing ever clipped)
  - 2026-07-24 status: reverted to the shipped 1.2.54 CSS (`max-height: 33vh`); DEF-12 OPEN. The symptom is minor (only at viewport < ~206px - a very short window / heavy zoom); a correct fix is a deliberate layout redesign (single body scroll with sticky headers, OR a shrink-capable layout with `all` given a content basis and a per-section min so nothing collapses) - a UX decision, not a quick patch. The v1.2.54 transparent-`::-webkit-scrollbar-track` rules remain in `style/base.css` and are a confirmed no-op (an isolation test proved a non-overflowing `scrollbar-gutter: stable` element paints no scrollbar on any platform) - safe to delete in a future cleanup
  - 2026-07-24 fixed (v1.2.58): layout redesign per plan - body `overflow: hidden auto` is the safety valve (scrolls only when section minimums cannot fit, so nothing is ever clipped); sections size to content with the cap on their list, `max-height: clamp(calc(5 * var(--ccs-row-height)), 33vh, calc(10 * var(--ccs-row-height)))`; `all` is `flex: 1 0 auto` (collapsed = shrink-proof header) and only when expanded (`:has` a list) `flex-shrink: 1` down to `calc(24px + 3 * var(--ccs-row-height))`; section headers `position: sticky` label the body-scroll state; `_render` saves/restores `_bodyEl.scrollTop` alongside the per-list positions; deleted the confirmed no-op v1.2.54 transparent-track rules. RE-SCOPED INVARIANT: a section of <= 5 rows never self-scrolls at ANY viewport height (the reported bug was 2 rows); 6-10 row sections yield to the 33vh mid-band and scroll internally BY DESIGN (ux round-1 finding - fixed caps pushed the headerless body-scroll state into ordinary window heights); the mid-row cut at the 33vh band is the deliberate scroll-continuation cue - do NOT re-file it as a regression
  - 2026-07-24 verified (live, classic scrollbars forced): 3 scenarios x 5 viewport heights (900/400/250/180/150) - Recent(2) 48/48 no self-scroll everywhere, Recent10/All95 at vh400 fits with no body scroll (caps yield to 132px), `all` floors at 97px with internal scroll and last-of-95-rows reachable incl. the 340x250 kill shot that ended the previous fix, Fav(15) caps at 240px, time-column edge identical across sections in every cell; bug-hunter's 28-cell sweep (heights x Recent 2/5/6/10) plus zoom 0.9-1.5 and clamp-boundary vh727 all clean; body scrollTop survives a real 30s poll re-render (40 -> 40)
  - 2026-07-24 review: adversarial panel architect + bug-hunter + ux-designer (Mode 2, live tree + live browser), 3 rounds to a clean pass - round 1 SHIP x3 with real findings, all fixed: body scrollTop reset on poll (architect MAJOR + bug-hunter, measured 218 -> 118), collapsed `all` squashed to 1px (bug-hunter), fixed 240px caps not yielding so body-scroll hit ordinary heights (ux MAJOR -> the clamp), sticky headers (ux), magic-number drift -> `--ccs-row-height` single source (architect); round 2 SHIP x3 - mid-row shear adjudicated KEEP per ux (continuation cue) and recorded in the CSS comment, prose/naming batch applied (test title, `--ccs` namespace, dead declarations, 33vh pinned in jest); round 3 architect confirm clean. jest 65/65, lint clean
  - 2026-07-25 re-report withdrawn: a screenshot (`cc-defect.png`) was sent post-1.2.59 as "still prevalent", but it was the pre-fix `problem-cc.png` capture renamed, not a fresh one - user confirmed false alarm and parked the investigation before the planned Galata reproduction instance was built; DEF-12 stays fixed, no reopen
  - 2026-07-31 re-reported with fresh evidence (`problem-1.png`, `problem-2.png`) and root-caused as a SECOND, independent cause - not a regression of the 1.2.58 layout fix, which holds: `.jp-ClaudeSessionsPanel-branchBadge` is `inline-flex` with `vertical-align: middle` inside the name span, and its text item inherited the row's `line-height: 24px`, making the badge exactly 24px tall; middle-alignment then grew the name span's line box to 25.09px in a fixed 24px row. Measured live via CDP against the real panel: any list whose LAST row carries a branch badge reports `scrollHeight 25` vs `clientHeight 24`, and on classic-scrollbar platforms that 1px of genuine overflow paints a full arrow-button scrollbar on a list that visually fits (the filtered 1-row Favourites in `problem-2.png`)
  - 2026-07-31 fixed (v1.2.69): `line-height: 1.3` on `.jp-ClaudeSessionsPanel-branchBadge` - the value the sibling `bgBadge` already carried, so the two inline badges now agree. Verified live with classic scrollbars forced: badge height 24px -> 12px, every list `scrollHeight == clientHeight` at viewport heights 900/400/250 filtered and unfiltered, vertical centring preserved (badge mid-Y 12.55 -> 13.09 of 24). Jest pins `line-height` on BOTH badges so neither can drift back; the popup's markers are flex children, where the mechanism cannot recur. Survived 5 adversarial rounds (architect + bug-hunter + ux-designer)

### DEF-13: a row whose conversation a background agent owns cannot be opened

- [x] clicking the `jupyterlab_resource_monitor_extension` row failed with `Session d47d74e9-... is currently running as a background agent (bg). Use ``claude agents`` to find and attach to it, or add --fork-session to branch off a copy` - the row was unopenable, and the session it named was the only one the panel offered for that project; cause: the panel elects a project's newest conversation as its representative and opens it with `claude --resume <id>`, but a conversation held by a live background agent (`/bg`, `claude --bg`) is refused by the CLI for as long as its worker lives - the panel had no notion of background agents at all; fix: `bg_agents()` asks `claude agents --json` which conversations live bg workers own; rows carry a `bg_id` for the marker, and `launch-terminal` resolves the verb at launch, running `claude attach <short>` instead of a resume when an agent owns the conversation being opened - the agent is joined, not copied, so nothing is lost; `jupyterlab_claude_code_extension/sessions.py`, `jupyterlab_claude_code_extension/routes.py`, `src/widget.ts`
  - 2026-07-26 reported: "when trying to open session from panel /home/lab/workspace/private/jupyterlab/jupyterlab_resource_monitor_extension i got this: Session d47d74e9-d323-4bbf-9b57-76eb171a6f45 is currently running as a background agent (bg)"; then "panel shows as the main session the session d47d74e9-... that is unrecoverable", "all sessions in the panel must be open-able", "we cannot have a session lost"
  - 2026-07-26 root-caused: `d47d74e9` is a live bg job (spawned 2026-07-25 22:03 from that project, `state: done` yet still held - supervisor pid 855237, worker 855330/855418 alive, in `~/.claude/daemon/roster.json` and `~/.claude/jobs/d47d74e9/state.json`); its JSONL is the project's newest, so `_resolve_latest` elects it. Reproduced verbatim on a throwaway probe agent: `claude --resume <probe-sid>` printed the identical refusal. Blast radius at the time: 2 rows unopenable (this one and `kolomolo/sales-proposals` via bg `39c74874`) out of 14 live bg agents
  - 2026-07-26 mechanism found: `claude attach <short>` ("Open the background session in this terminal ... The session keeps running either way") is the true open - no fork, no copy. Probed live: the attach client is a bare `claude attach <short>` process, writes NO `~/.claude/sessions/<pid>.json` and has no children, so argv is the only handle on its conversation - hence `_parse_attach_id` + `_expand_short_session_id` feed `_claude_session_id`, without which every click would spawn another attach terminal
  - 2026-07-26 detector chosen: `claude agents --json` (~0.4s, one call per poll, 5s timeout, degrades to "no agents" on any failure) over reading `~/.claude/daemon/roster.json` - it is the same authority that refuses the resume, so the panel's verdict cannot disagree with the CLI's; user's call
  - 2026-07-26 fixed: `attach_id` on `launch-terminal` builds `[claude, attach, <short>]` and drops the flags attach cannot take (the agent's permission mode and name were fixed at spawn, so `Resume (Skip Permissions)` is disabled for such a row and the open item reads `Attach to Background Agent`); a bg-owned row carries a quiet `bg` chip plus a tooltip naming the agent, and a bg-owned branch reads `[bg]` in the branch list; `_openBranch` takes the branch's OWN `bg_id` rather than inheriting the row's (which would attach to the wrong agent); pytest 138, jest 72, tsc + lint clean
  - 2026-07-26 review round 1 (architect + bug-hunter + slop-hunter, Mode 2 on the live tree): DO-NOT-SHIP x3, 17 findings, every one triaged against the code and the CLI. Two MAJORs were real holes in the fix: (1) the Manage Sessions popup's pinned current row still resumed - `list_branches` excludes the current conversation from `branches`, so the branch lookup missed it and fell back to `--resume`, reproducing the refusal on that surface; opening it now delegates to the row itself. (2) the verb was chosen from a panel flag up to 30s stale, so clicking a project within a poll of a `/bg` still printed the verbatim refusal, and clicking after an agent exited attached to nothing; the decision moved SERVER-side into `launch-terminal`, which asks `bg_agents()` at launch - `attach_id` left the wire entirely and `bg_id` is now display-only. Two more MAJORs were real defects of my own making: the new `subprocess` ran inline in two synchronous Tornado handlers (measured 0.149s -> 0.491s on a listing the fork watcher polls every 2s for 3 minutes), now `run_in_executor`; and a stale `~/.claude/sessions/<pid>.json` outranked the attach argv - 2733 never-pruned pid files on this machine, and an attach client writes none, so a leftover would have won permanently and focused this terminal for an unrelated row (DEF-8/DEF-11 classes) - attach is now read FIRST
  - 2026-07-26 review round 1 slop cut (all mutation-proven by the reviewer, re-proven by me): a dead `real_bg_agents` conftest monkeypatch whose docstring asserted an ordering rule that never existed (6 tests pass without it, in 0.04s); a test-only `claude_binary` parameter on `bg_agents`; a jest test pinning the chip's exact colour tokens (a recolour turned CI red with zero behaviour change); the suite's only `.length).toBe()` count assertion; and the chip's own `title`, which shadowed the row tooltip and hid path plus session id behind a two-character chip. Also corrected a fabricated comment - "appending a flag would break the launch outright" is false, `claude attach <id> --dangerously-skip-permissions` prints `warning: extra arguments ignored`; and `_expand_short_session_id` now compares conversation ids rather than paths, since a renamed project leaves the same conversation under two encoded dirs (live instance in this tree) and was being called ambiguous
  - 2026-07-26 review rounds 2 and 3 (same three reviewers, resumed): round 2 found the round-1 client-side fix had become dead code once the verb moved server-side - the architect retracted its own round-1 prescription and the slop-hunter proved the inertness by mutation, so `_openBranch` reverted to its pre-change one-liner and two jest contracts went with it. The bug-hunter then measured what `list_branches` was paying for its `[bg]` branch marker: 0.375s per call, 100% of a branches request, plus 0.35s before every right-click and up to 34s of CPU per fork across the fork watcher's 90 polls - all for a marker never asked for, so branch-level `bg_id` was deleted outright (`IBranch.bg_id`, `_branchDisplayName`'s suffix, one pytest, one jest). The popup's pinned current-row marker went too: it would have been the only markable row in a list whose others can no longer be marked, implying they are clean when their status is unknowable. Marking is panel rows only. Also corrected three rounds of doc drift in `docs/acc-crit-jupyterlab_claude_code_extension.md`, where each design revision had left an `[x]` criterion describing a surface that no longer existed - the reviewers' standing lesson: when a surface is deleted, grep the acceptance criteria for it before closing the round
  - 2026-07-26 recorded option, NOT implemented: if branch-level marking is ever wanted back, the zero-subprocess route is to have `list_sessions` (which already calls `bg_agents()` once per poll and already walks each project dir) carry that project's set of agent-owned conversation ids, so branch labels can be marked from the row payload with no extra call. Do NOT reinstate the per-branch lookup in `list_branches` - that is the 34s-per-fork cost this defect removed
  - 2026-07-26 review rounds 4 and 5: all three reviewers SHIP. Round 4 restored two behavioural jest contracts that a deletion had taken by accident (the row chip - including the assertion that the chip sets no `title` of its own - and the menu label plus disabled skip-permissions), caught independently by the architect and the bug-hunter; the slop-hunter's residue sweep came back clean across helpers, imports, CSS and call sites. Separately, the live check of a branch-owned agent (`78e82229`, in the reported project) surfaced a pre-existing state that is NOT this defect: an agent stopped before its first response finished leaves a stub JSONL, so `claude attach` answers `Couldn't wake ... no saved transcript` and `claude --resume` answers `No conversation found with session ID` - it was unopenable before this fix and remains so, and claude's own remedy is `claude respawn`, which the panel deliberately does not run. Recorded as an acc-crit edge rather than fixed
  - 2026-07-26 review closed: architect, bug-hunter and slop-hunter all SHIP. Final live confirmation on the shipped build covered both shapes - a ROW's conversation (`39c74874`, via a browser click: chip rendered, `claude attach 39c74874` launched, a second click reused the terminal) and a BRANCH-only agent (`5d08f692`, 2.69 MB transcript, no marker shown anywhere: `claude attach 5d08f692` launched and the terminal identified back to its full conversation id). Both agents plus the stub survived every detach. One claim of mine was corrected by the bug-hunter in the process: I had asserted both branch-only agents were transcript-less, when only `78e82229` is - the same unverified-claim class the reviewers spent three rounds removing from the code comments

### DEF-14: a background job with no live worker gets a false bg chip and an attach that resurrects it

- [x] a foreground, actively-used session was marked `bg` in the panel; cause: `bg_agents()` accepted every `claude agents --json` entry with `kind == "background"`, but that listing is derived from the on-disk job records under `~/.claude/jobs/`, not from the daemon - it keeps listing a job whose worker exited (`state: "blocked"`, i.e. waiting on the user), so membership does not mean a resume would be refused; the false chip then made `launch-terminal` pick `claude attach <short>`, which RESURRECTS the dead worker under the job's stored `respawnFlags` and thereby makes the chip true; fix: an entry counts only when it reports a worker `pid` that still exists (`_pid_alive`); `jupyterlab_claude_code_extension/sessions.py`
  - 2026-08-01 reported: "/home/lab/workspace/private/games/stars-ultranova-web - the main session is fg, and active, why marked as bg ?"
  - 2026-08-01 first analysis was WRONG and was refuted by review: DEF-13's premise "listed in `claude agents --json` iff a resume would be refused" was taken as sound, and the report was about to be closed as correct-by-design on the strength of two measurements - a LIVE agent refusing `--resume` verbatim, and a SETTLED (`state: done`, unlisted) job resuming clean. Neither probes the gap: `blocked`
  - 2026-08-01 root-caused: the reviewers probed the missing case. Live census: 17 listed background entries, exactly 1 carrying a `pid` (the only claude worker in `ps`); `list_sessions` marked 3 rows, and `claude --resume` succeeded with no refusal on 2 of them (`d4660d44`, `39c74874`, both `state: blocked`, no pid, no process). Listed does NOT imply refused - liveness is the real predicate
  - 2026-08-01 mechanism of the user's own row: job `48eab74a` ("stars-ultranova", created 2026-07-28 via `/bg`) blocked on the weekly limit; the daemon idle-exited 2026-07-31T00:11Z (`cause=idle_exit, live_workers=0`) leaving the job workerless but still listed, so the chip stayed lit through an 18-hour dead window; at 2026-07-31T18:09:46Z the panel launched `claude attach 48eab74a` (proven from pid 267854's environ carrying `JUPYTERHUB_API_TOKEN` and `JUPYTER_SERVER_URL`), which spawned a transient daemon and respawned the worker with the job's stored `--effort high --permission-mode auto --model claude-fable-5[1m]`. The user's foreground terminal is that attach client, so the row is genuinely bg NOW - the chip was self-fulfilling
  - 2026-08-01 fixed: `bg_agents()` requires `isinstance(pid, int) and _pid_alive(pid)` alongside `kind == "background"`; the docstring's false "same authority that refuses the resume" invariant replaced with what was measured. Live re-check: marked rows 3 -> 1, and the one that remains is the single live worker. pytest 138 (the old fixture asserted a pid-less `state: done` entry WAS owned - it pinned the defect; two tests added for the workerless and stale-pid cases)
  - 2026-08-01 review round 2 (architect + bug-hunter on the fix): SHIP x2. The bug-hunter read the CLI bundle and proved the predicate is now the SAME authority as the refusal - `agents --json` publishes `pid` only for a worker `Oze()` has verified alive and start-time-matched, and the resume guard `gDe()` is `listAllLiveSessions() -> Oze` - so no state exists where claude refuses and the panel picks `--resume`; during worker startup neither side sees the record, so claude permits the resume too. Both reviewers mutation-tested the new tests (deleting the predicate fails exactly the two added cases). One MINOR accepted: the code comment's stated mechanism was wrong in both clauses (the pid is published off the validated worker record, not off a hosting daemon; the listing's stale pids sit on interactive entries and point at live unrelated processes, not dead ones) - comment corrected, no behaviour change

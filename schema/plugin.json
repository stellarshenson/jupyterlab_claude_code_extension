{
  "title": "Claude Code Sessions",
  "description": "Settings for the Claude Code sessions side panel.",
  "type": "object",
  "jupyter.lab.setting-icon": "jupyterlab_claude_code_extension:claude",
  "jupyter.lab.setting-icon-label": "Claude Code Sessions",
  "properties": {
    "presentationMode": {
      "type": "string",
      "title": "Presentation mode",
      "description": "How session rows are labelled. `name` (default) shows the session name Claude records for the project (so a `/rename` shows through); the initial name is taken from the project folder name, then the path is walked up to disambiguate when two projects share a folder name. `path` shows the project path relative to the JupyterLab notebook root (absolute if outside it).",
      "enum": ["name", "path"],
      "default": "name"
    },
    "recentLimit": {
      "type": "integer",
      "title": "Recent items count",
      "description": "Number of rows shown in the Recent section, sorted by last activity (descending).",
      "default": 10,
      "minimum": 1,
      "maximum": 100
    },
    "dangerouslySkipPermissions": {
      "type": "boolean",
      "title": "Dangerously skip permissions",
      "description": "When enabled, newly spawned and resumed sessions are launched with `--dangerously-skip-permissions`. Bypasses every Claude Code permission prompt - only enable in sandboxes you trust.",
      "default": false
    },
    "sidebar": {
      "type": "string",
      "title": "Sidebar position",
      "description": "Which JupyterLab sidebar to dock the panel to. `left` (default) keeps it next to the file browser. `right` puts it opposite. Takes effect on the next JupyterLab reload.",
      "enum": ["left", "right"],
      "default": "left"
    }
  },
  "additionalProperties": false
}
